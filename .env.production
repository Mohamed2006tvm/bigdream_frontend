# Used automatically by `vite build` (and Vercel production builds).
# Must match your live API — this value is baked into the JS bundle.
VITE_API_URL=https://bigdream-backend.vercel.app/api
VITE_APP_ENV=production
